[build]
builder = "NIXPACKS"

[deploy]
releaseCommand = "cd server && npx sequelize-cli db:migrate"
startCommand = "cd server && npm start"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
