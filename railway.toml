[build]
builder = "NIXPACKS"
buildCommand = "cd server && npm install --prefer-offline"

[deploy]
startCommand = "cd server && npm run start:prod"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
